# 参考关卡保真度差异审计

本审计把“工程验证完成”和“原作几何/主观手感真人确认”分开。自动生成、机制等价映射或公开拓扑信号可证明本地白盒合同与机制覆盖，但不能替代合法游戏体验下的逐房几何、节奏和手感对照。

## 当前分层

| 指标 | 数量 |
|---|---:|
| 必需条目 | 908 |
| 手工代表白盒 | 15 |
| 目录驱动首轮白盒 | 893 |
| 自动结构/编译通过 | 908 |
| 浏览器逐房状态通过 | 908 |
| 工程 validation 通过 | 908 |
| continuousRun 通过 | 908 |

Celeste 的公开净化尺寸/出生点信号覆盖 804/804 房；这些信号只用于比例和候选拓扑。Ori 的 104 个分区全部是原创本地加载边界，不是官方房间。

机制使用映射：7114/7114 条条目-机制使用已有 Cablester 等价系统；其中 7114 条在对应 JSON 中实际存在映射对象或能力。未映射不等于缺少关卡文件，而是保真度阻塞项。

## 尚无明确等价映射的机制（0）

| 机制 | 涉及条目 | 游戏 | 后续动作 |
|---|---:|---|---|
| 无 | 0 | — | — |

## 已有映射但 JSON 尚缺对应对象（0 个机制族）

| 机制 | 预期映射 | 缺少条目 / 涉及条目 | 缺少对象的房间 |
|---|---|---:|---|
| 无 | — | 0/0 | — |

## 完成门槛

- 每个条目的主路线、可选连接、尺寸信号、出生点、危险物和状态变化都有显式来源、近似说明或待真人确认项；
- 浏览器综合验收覆盖每房入口、出口目标、死亡重置、资源恢复、渲染和重进，连续审计以实际输入完成每个集合的主路线；
- 每个 Side/区域连续通关并记录性能；
- 机制映射、连接、自动测试、浏览器综合验收、连续主路线和性能证据全部与当前内容指纹一致后，`validation` 才可升为 `validated`；原作几何和主观手感继续单列 `humanConfirmation=needed`。
